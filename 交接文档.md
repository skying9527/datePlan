# 拾光任务 —— 项目交接文档

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 拾光任务（任务规划与记录网页） |
| 文档版本 | v1.0 |
| 文档日期 | 2026-08-15 |
| 项目位置 | `C:\Users\sky\Desktop\code\AI\plan project` |
| 交接对象 | 接手本项目的开发者 / 后续会话 |
| 关联文档 | 《需求分析文档》v1.3 · 《设计方案》v1.0（同目录） |

> 阅读顺序建议：先读本交接文档（总览），再按需查阅《需求分析文档》（为什么这样做）与《设计方案》（正式版怎么做）。

---

## 1. 项目概览

**一句话**：本地部署、纯前端的个人任务规划与记录网页——老黄历视觉 + 泳道式横向时间线，含日历、当天时间线、任务清单、每日总结、激励弹幕，数据本地持久化。

**当前状态**：
- ✅ 需求已定稿（v1.3），多轮原型迭代完成并经过用户验收
- ✅ 视觉与交互定稿：**老黄历皮肤 + 泳道式时间线**
- ✅ 正式版技术方案已确认：**Vue 3 + Vite + 本地静态服务**，并已为未来**多用户/并发/账号**预留架构接缝
- ⏳ 下一步：按《设计方案》§14 启动 **M2 正式版工程骨架**

**最终形态**：一个可交互的单文件 HTML 原型（`原型A-老黄历-泳道式/index.html`），双击即可体验全部功能。

---

## 2. 需求与决策历程

### 2.1 版本历程
| 版本 | 阶段 | 关键内容 |
| --- | --- | --- |
| v1.0 | 初始 | 需求澄清：固定时长任务=单次+重复都要、长期任务=可配重复频率、持久化=localStorage、原型=可交互 HTML |
| v1.1 | 首次改版 | 弹幕改底部舞台（不覆盖）且仅三时机触发；三套皮肤（老黄历/奶油/原木）；任务清单页；备注+完成情况+每日总结（按周汇总）；优先级三级；删除确认；当天页整页 |
| v1.2 | 二次改版 | 选定老黄历；日历点开先弹侧边栏；当天页=列表+横向时间线（A 泳道/B 堆叠/C 看板三变体供选）；任务可编辑；新增按钮放日期栏最右 |
| v1.3 | 定稿 | 选定**泳道式**；全天任务独立区（始终可见）；矩形框放大+内容重排；右边缘把手**调整时长**；顶栏"今日详情"入口；优先级选择加固；**勾选=立即完成变灰、修改=独立编辑** |

### 2.2 关键确认结论（重要，勿再改）
1. 三类任务：**一次性**（日期+可选时间）、**固定时长**（时长必填，单次或按频率重复）、**长期**（每天/每周若干天）；
2. 优先级三级：**紧急（红）/ 重要（橙）/ 普通（蓝灰）**，紧急排前；
3. **勾选 = 立即完成/取消**（马上变灰，无弹窗）；**修改 = 独立编辑弹窗**（全字段预填）；
4. 删除必须**确认弹窗**（不可恢复警示）；
5. 每日总结**按周分组**汇总；任务可写备注；
6. 弹幕仅在「打开侧边栏/进入当天页、完成任务、保存任务」三时机触发，**底部舞台不覆盖内容**；
7. 无开始时间的任务显示在**全天独立区**（时间轴上方，不随横向滚动）；
8. 时间线拖拽：**主体拖动=改开始时间（吸附15分钟）/上下换泳道**；**右边缘把手=拉长缩短时长**；
9. 正式版技术栈：**Vue 3 + Vite**；运行：**本地静态服务**（http://localhost，一键启动脚本）；
10. 演进：**仅前端预留**（后端后议），未来场景个人多设备与小团队**两者兼容**。

---

## 3. 当前技术方案（定稿摘要）

| 维度 | 方案 |
| --- | --- |
| 视觉 | 老黄历：纸感底色 `#F2E8D0`、朱砂红 `#A03028`、楷体/宋体、农历+干支+宜忌 |
| 时间线 | 泳道式横向 0–24h（1h=90px，行高64px），重叠任务自动上下分层，全天任务独立区 |
| 原型 | 单文件 HTML（`原型A-老黄历-泳道式/index.html`），原生 JS，localStorage |
| 正式版 | Vue 3（Composition API）+ Vite + Pinia + vue-router(hash) + 原生 CSS tokens |
| 运行 | `http://localhost` 静态服务（构建产物 dist/ 也可双击兼容） |
| 存储 | 统一 **Promise 契约**存储适配层；LocalAdapter（现在）/ RemoteAdapter（未来） |
| 演进预留 | UUID 任务 ID、`version` 乐观锁字段、`ui.user` 登录态占位、schema 迁移链 |

---

## 4. 交付物清单（当前目录）

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `需求分析文档.md` | 文档 v1.3 | 需求定稿：功能需求、澄清记录、数据模型、验收要点、里程碑 |
| `设计方案.md` | 文档 v1.0 | 正式版详细设计：架构/数据/组件/泳道算法/视觉 tokens/测试/实施计划/**§17 演进预留** |
| `交接文档.md` | 文档 v1.0 | 本文档 |
| `原型A-老黄历-泳道式/index.html` | 可交互原型 | **最终定稿原型**，双击即用，功能与正式版目标 1:1 |

---

## 5. 最终原型功能清单（验收核对用）

- [x] 日历月视图：农历+干支+宜忌（示例规则）、优先级色任务标签、翻月/回到今天、今日高亮
- [x] 点击某天 → **侧边栏**：当天任务（勾选即变灰/编辑/删除）+「查看当天详情」跳整页
- [x] 当天页：顶部日期栏（**添加当天任务按钮在最右**）+ 实时时钟 → 今天任务列表 → **全天任务独立区** → **泳道时间线** → 每日总结
- [x] 泳道时间线：重叠任务上下分层不遮挡；**主体拖动改时间/换泳道**；**右边缘把手调整时长**；当前时间红线实时移动；打开自动滚动到当前时刻
- [x] 任务管理：三类任务、优先级三级、备注、**新增/编辑（全字段预填）/删除（确认）/完成（立即变灰）**
- [x] 任务清单页：状态/类型筛选、紧急优先排序、展开详情（备注+完成记录）
- [x] 每日总结：当天可写可存；汇总页**按周分组**，未写日期点击跳转补写
- [x] 顶栏：Tab[日历|任务清单|每日总结|**今日详情**] + 今日进度 + 预览激励
- [x] 弹幕激励：三时机触发，底部舞台升起淡出，不覆盖内容
- [x] 持久化：localStorage，刷新/重启恢复；存储不可用时降级内存（页面仍可用）

---

## 6. 关键实现细节（正式版需复刻）

### 6.1 泳道时间线参数（`utils/timeline.js` 常量）
```
HOUR_W=90px（1小时宽，24h 总宽 2160px，横向滚动）
LANE_H=64px（行高，矩形框高 58px）· TOP=36px（刻度区）
SNAP_MIN=15 分钟（拖拽/调整吸附步长）· MIN_DUR=15 · MIN_W=64px
```

### 6.2 泳道分配算法
贪心：任务按开始时间排序，依次放入"与已有任务时间段不重叠"的最小可用行（偏好 `laneIdx`，冲突自动顺延）→ 保证最少行数。纯函数，可单测。

### 6.3 拖拽交互（pointer 事件）
- 命中优先级：`.mini` 按钮（点击）→ `.tl-resize` 把手（调整时长）→ `.tl-task` 主体（移动/换泳道）；
- 移动：改 `startTime`（`endTime` 平移保持时长）+ `laneIdx`；
- 调整时长：改 `durationMin`（fixed 任务）+ `endTime`；吸附 15min、最小 15min、不超 24:00；
- 监听器挂 `document`，结束后移除；**重渲染先移除旧监听再绑定**（原型中曾修复监听器累积 bug）。

### 6.4 农历模块（`utils/lunar.js`，已验证）
- `lunarInfo` 表 150 项（1900–2049）+ `solar2lunar`；
- 验证基准：1900-01-31=正月初一、**2026-08-18=丙午年七月初六**、2000-01-01=戊午日、2026 干支年=丙午；
- 干支日公式 `(JDN+49)%60`；宜忌为 10 组循环示例表（接口 `yiJiOf(iso)`，正式版可换真实数据源）。

### 6.5 存储与数据（正式版 v3）
- 任务模型：`id(UUID) / title / type / date / startTime / endTime / durationMin / mode / repeat / priority / note / completed{日期:{at,note}} / laneIdx / version / createdAt / updatedAt`；
- 每日总结：`{ "2026-08-15": "文本" }`；
- Key：`planApp.tasks.v3`、`planApp.notes.v3`、`planApp.meta.v3`；迁移链 `v2→v3`（备份旧 key、不覆盖用户数据、种子仅全新安装写入）；
- **接口一律 Promise 风格**（`await taskRepo.loadAll()` 等），LocalAdapter 同步实现直接 resolve——这是未来切后端的接缝。

### 6.6 弹幕系统
- 底部 fixed 舞台（约 64px，`pointer-events:none`），弹幕升起-上浮-淡出约 3.2s，随机横向位置；
- 语句库集中管理（`utils/quotes.js`）：查看/夸赞/鼓励/欢迎四组。

### 6.7 防护与健壮性（原型已实现，正式版保留）
- `localStorage` 访问全部 try/catch，不可用时降级内存并提示；
- 初始化整体 try/catch，出错在页面顶部显示红色错误条（便于定位）；
- 时钟定时器只在当天页激活时运行，内部 try/catch。

---

## 7. 测试与质量现状

- **历轮交互测试**：累计 100+ 项断言全部通过（模拟 DOM 环境），覆盖：日历渲染、侧边栏（含编辑按钮/勾选即时变灰）、当天页、泳道（全天区/分层）、拖拽移动/换泳道、**右边缘调整时长**、新增/编辑/删除确认、优先级保存、筛选排序、按周汇总、每日总结、Esc、弹幕触发时机、监听器去重；
- **农历算法**：4 项真实基准校验通过；
- **严格模式扫描**：确认无"裸赋值"（曾因 `'use strict'` 下给未声明变量赋值导致真实浏览器空白，已修复为 `var` 声明）；
- **测试盲区说明**：Node vm 环境对"函数表达式赋值给未声明变量"存在兼容性盲区（不抛错），因此测试脚本额外增加**静态扫描**兜底；真实浏览器需人工验证关键路径；
- 沙箱环境无法启动无头浏览器，真实浏览器验证由用户执行（打开原型即可）。

---

## 8. 已知问题与注意事项

1. **file:// 打开有安全警告**：直接双击 HTML 时浏览器 Console 可能出现 `Unsafe attempt to load URL file:///...`（非致命）。正式版改为本地静态服务后消失；
2. **浏览器缓存**：修改原型后请 **Ctrl+F5 强刷**，避免看到旧版本（曾发生因旧缓存导致误判"页面坏了"）；
3. **严格模式崩溃已修复**：若未来改动 JS，勿再对未声明变量赋值（文件第一行 `'use strict'`）；
4. **宜忌为示例数据**：老黄历宜忌目前是 10 组循环示例，非真实黄历规则，正式版可接真实数据源（接口已预留）；
5. **localStorage 与浏览器绑定**：清除浏览器数据会丢数据，正式版 M5 提供导出/导入 JSON 备份；
6. **数据兼容**：原型用 `planApp.tasks.v2`，正式版 v3 通过迁移链自动升级，用户已有数据不覆盖。

---

## 9. 演进路线（已确认）

- **当前**：本地单用户（M2–M5 实现正式版，含 Promise 存储契约 / UUID / version / user 占位）；
- **未来阶段 1**：个人多设备同步 → RemoteAdapter + Node.js + SQLite（后端后议）；
- **未来阶段 2**：小团队多用户 → 完整账号（JWT）+ 乐观锁冲突合并 + PostgreSQL（可选）；
- 详见《设计方案》§17：接缝对照表、并发原则、后端选型、接入核对清单。

---

## 10. 后续开发计划（按《设计方案》§14）

| 阶段 | 内容 | 说明 |
| --- | --- | --- |
| **M2 骨架** | Vite 工程 + 路由 + tokens/base.css + **Promise 存储层（迁移/UUID/version）** + Pinia + 农历/日期工具 | 待启动（下一步） |
| M3 四视图 | 日历+侧边栏、当天页（列表+全天区+泳道时间线+拖拽/调整时长+时钟+总结）、清单、按周汇总；任务表单、删除确认 | 功能与原型 1:1 |
| M4 体验 | 弹幕系统、动效打磨、今日进度、预览激励 | 完整正式版 |
| M5 收尾 | 无障碍/响应式走查、导出/导入 JSON、构建产物双击兼容 | 发布版 |

依赖：M3→M2，M4→M3，M5→M4。

---

## 11. 环境与工具依赖

| 项 | 说明 |
| --- | --- |
| 运行环境 | 浏览器（Edge/Chrome 等现代浏览器）；原型双击即用 |
| 开发环境 | Node.js（含 npm）——正式版 Vite 工程所需；当前目录已可用 `node` |
| 操作系统 | Windows（开发/验证均在 Windows 完成） |
| 验证手段 | 原型=人工打开 + Node 模拟 DOM 测试脚本（临时脚本，用后即删）；正式版=Vitest + Vue Test Utils |

---

## 12. 交接核对清单（接手后建议先做）

- [ ] 双击打开 `原型A-老黄历-泳道式/index.html`，确认日历/侧边栏/当天页/泳道/清单/总结/弹幕全部可用；
- [ ] 新增一个**不填时间**的任务 → 确认出现在"全天"独立区；
- [ ] 拖动矩形框**主体**改时间/换泳道；拖**右边缘把手**调整时长；
- [ ] 勾选任务 → 立即变灰；点编辑 → 弹窗预填；删除 → 确认弹窗；
- [ ] 顶部「今日详情」直达今天；「每日总结」页按周分组；
- [ ] 刷新/重启浏览器 → 数据仍在；
- [ ] 通读《需求分析文档》v1.3 与《设计方案》v1.0（重点 §17 演进预留）；
- [ ] 按 §14 启动 M2 工程骨架。

---

## 13. 文档索引

| 文档 | 用途 | 何时读 |
| --- | --- | --- |
| 交接文档.md（本文） | 项目总览与接手指引 | 接手时 |
| 需求分析文档.md | 需求定稿与验收要点 | 开发前/验收时 |
| 设计方案.md | 正式版详细设计（含演进预留） | 实施 M2–M5 时 |
