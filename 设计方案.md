# 拾光任务 —— 详细设计方案

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 关联文档 | 《需求分析文档》v1.3（已定稿） |
| 技术栈 | **Vue 3（Composition API）+ Vite**（已确认） |
| 运行方式 | **本地静态服务** `http://localhost`，一键启动脚本（已确认） |
| 视觉基准 | 老黄历皮肤 + 泳道式横向时间线（以最终原型 `原型A-老黄历-泳道式/index.html` 为基准） |

---

## 1. 设计目标与约束

### 1.1 目标
把已定稿的可交互原型工程化为**可维护、可扩展、长期使用的正式版**：
1. 功能与最终原型 1:1 对齐（日历/侧边栏/当天页/泳道时间线/清单/总结/弹幕/持久化）；
2. 组件化、模块化，后续迭代成本低；
3. 本地部署、无后端、数据本地持久化、重启可恢复；
4. 保留老黄历视觉与"舒适、不正规"的基调。

### 1.2 约束
| 约束 | 说明 |
| --- | --- |
| 单人本地使用 | 无多用户、无并发、无账号体系 |
| 纯前端 | 不引入后端服务，构建产物由静态服务托管 |
| 数据规模小 | 个人任务量（数百条以内），全量渲染即可，无需虚拟列表 |
| 数据可迁移 | 原型已有 `planApp.tasks.v2` 数据，正式版需兼容迁移 |
| **可演进性** | **面向未来多用户/并发/账号机制预留架构接缝（当前不实现，见 §17）** |

---

## 2. 总体架构

### 2.1 技术栈明细
| 层 | 选型 | 说明 |
| --- | --- | --- |
| 框架 | Vue 3 + Vite | Composition API + `<script setup>` |
| 状态 | Pinia | 官方推荐，任务/总结/UI 三个 store |
| 路由 | vue-router（hash 模式） | `#/calendar` 等，file:// 与 http:// 均可 |
| 样式 | 原生 CSS + CSS 变量（tokens） | 无 UI 框架，保持老黄历定制外观；不引入 Tailwind 以免稀释主题 |
| 存储 | localStorage + 自研适配层 | 默认 localStorage，预留 IndexedDB 接口 |
| 测试 | Vitest + Vue Test Utils；Playwright（可选 E2E） | 与 Vite 同生态 |
| 构建/启动 | Vite + 启动脚本 | `scripts/start` 一键起本地服务 |

### 2.2 分层架构
```
┌─────────────────────────────────────────────────┐
│ 视图层（页面组件）                                 │
│  CalendarView · DayView · TasksView · NotesView  │
│  AppHeader · SidePanel · DanmakuStage · 弹窗      │
├─────────────────────────────────────────────────┤
│ 业务层（Pinia stores + composables）              │
│  useTasksStore · useNotesStore · useUIStore      │
│  useDanmaku · useTimeline · useTaskForm          │
├─────────────────────────────────────────────────┤
│ 领域/工具层                                       │
│  date.js · lunar.js · priority.js · quotes.js    │
├─────────────────────────────────────────────────┤
│ 存储层（storage adapter —— 演进接缝，见 §17）     │
│  LocalAdapter（现在，Promise 接口）               │
│  RemoteAdapter（未来，HTTP 实现同一契约）          │
└─────────────────────────────────────────────────┘
```

### 2.3 数据流
```
用户操作 → 组件事件 → store action
  → 存储层写入（localStorage，立即持久化）
  → store 状态更新 → 视图响应式重渲染
  → （弹幕等副作用由 store action 触发）
```

### 2.4 演进预留总览（详见 §17）
| 接缝 | 现在（M2 落实） | 未来（多用户/并发/账号） |
| --- | --- | --- |
| 存储接口 | 统一 **Promise（异步）** 契约 | 新增 RemoteAdapter 实现同一契约，业务层零改动 |
| 任务 ID | **UUID**（全局唯一） | 多人各自生成再合并也不冲突 |
| 并发控制 | 模型含 `version`（=1） | 乐观锁：比对版本，冲突提示合并 |
| 登录态 | `user = null`（本地模式） | 登录后注入用户信息，路由守卫保护 |
| 数据归属 | 本地模型无 owner 维度 | 后端按登录会话隔离，前端不感知 |

---

## 3. 项目目录结构

```
plan-app/
├── index.html
├── package.json
├── vite.config.js
├── scripts/
│   ├── start.mjs            # 一键启动：npm run start → 打开 http://localhost:5173
│   └── build.mjs            # 构建产物 dist/
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── router/
│   │   └── index.js         # hash 路由
│   ├── styles/
│   │   ├── tokens.css       # 老黄历设计 tokens（CSS 变量）
│   │   └── base.css         # 基础结构样式（布局/组件通用）
│   ├── stores/
│   │   ├── tasks.js         # 任务 store
│   │   ├── notes.js         # 每日总结 store
│   │   └── ui.js            # 路由态/筛选/弹窗/侧边栏状态
│   ├── storage/
│   │   ├── adapter.js       # 存储适配层入口（统一 Promise 契约，见 §4.3/§17）
│   │   ├── keys.js          # 存储 key 常量
│   │   ├── migrations.js    # v2→v3… 迁移函数
│   │   └── remote/          # 【未来】RemoteAdapter（HTTP 实现同一契约，当前不实现）
│   ├── utils/
│   │   ├── date.js          # 日期工具（ISO/加减/格式化）
│   │   ├── lunar.js         # 农历/干支/宜忌（已验证算法）
│   │   ├── priority.js      # 优先级排序与标签
│   │   ├── timeline.js      # 泳道分配算法（纯函数，可单测）
│   │   └── quotes.js        # 弹幕语句库
│   ├── composables/
│   │   ├── useDanmaku.js
│   │   ├── useTimelineDrag.js
│   │   └── useTaskForm.js
│   └── components/
│       ├── layout/          # AppHeader.vue · DanmakuStage.vue
│       ├── calendar/        # CalendarView.vue · DayCell.vue · SidePanel.vue
│       ├── day/             # DayView.vue · DayTaskList.vue · LaneTimeline.vue
│       │                    #  TimelineBar.vue · AllDaySection.vue · DailyNote.vue
│       ├── tasks/           # TasksView.vue · TaskRow.vue
│       ├── notes/           # NotesView.vue · WeekGroup.vue
│       └── common/          # TaskFormModal.vue · ConfirmDialog.vue
│                            # PriorityBadge.vue · Icon.vue · EmptyTip.vue
```

---

## 4. 数据设计

### 4.1 任务模型（v3）
```json
{
  "id": "UUID（全局唯一，见 4.3）",
  "title": "写周报",
  "type": "once | fixed | long",
  "date": "2026-08-15",              // once / fixed单次：必填
  "startTime": "09:00", "endTime": "10:00",   // 可选；endTime 可由时长/拖拽派生
  "durationMin": 30,                 // fixed：必填；拖拽 resize 时同步更新
  "mode": "once | repeat",           // fixed 的执行方式
  "repeat": { "kind": "daily" } | { "kind": "weekly", "days": [1,3,5] }, // 0=周日…6=周六
  "priority": "urgent | important | normal",   // 默认 normal
  "note": "备注/心得（可选）",
  "completed": { "2026-08-15": { "at": "ISO", "note": "" } },  // 按天完成记录
  "laneIdx": 0,                      // 泳道偏好（可选，拖拽换行时写入）
  "version": 1,                      // 乐观锁版本号【演进预留：多用户并发冲突检测，见 §17】
  "createdAt": "ISO",
  "updatedAt": "ISO"
}
```
字段规则（与原型一致）：
- 一次性：`date` 必填，时间可选；
- 固定时长：`durationMin` 必填；`mode=once` 需 `date`，`mode=repeat` 需 `repeat`；
- 长期：`repeat` 必填（每天或每周至少 1 天）；
- 完成语义：`completed[日期]` 存在即完成；勾选完成写入 `{at, note:''}`；再勾选删除该键；
- 时间语义：`endTime` 优先于 `durationMin` 参与宽度计算（`taskDur = endTime-startTime || durationMin || 60`）；
- **归属语义**：本地模型**不携带 owner 维度**；多用户阶段由后端按登录会话隔离（见 §17），前端本地数据保持干净。

### 4.2 每日总结模型
```json
// 独立存储对象：{ "2026-08-15": "总结文本" }
{ "2026-08-15": "今天完成了……" }
```

### 4.3 存储层设计
| Key | 内容 | 版本 |
| --- | --- | --- |
| `planApp.tasks.v3` | 任务数组 JSON | v3 |
| `planApp.notes.v3` | 每日总结对象 JSON | v3 |
| `planApp.meta.v3` | 元信息（如迁移标记、首次使用日期） | v3 |

适配层接口（`storage/adapter.js`）—— **统一 Promise（异步）契约**：
```js
// 底层（LocalAdapter 内部同步实现，直接 resolve；RemoteAdapter 未来实现为 HTTP）
read(key): Promise<any>
write(key, value): Promise<void>
isAvailable(): boolean                 // localStorage 可用性探测（file:// 受限环境降级为内存）

// 任务仓库 taskRepo（业务层一律 await，未来切远端零改动）
loadAll(): Promise<Task[]>
saveAll(tasks): Promise<void>
add(task): Promise<void>
update(task): Promise<void>
remove(id): Promise<void>
toggleDone(id, iso): Promise<void>     // 完成/取消完成，立即持久化

// 总结仓库 noteRepo
get(iso): Promise<string>
set(iso, text): Promise<void>
remove(iso): Promise<void>
```
- **降级策略**：`localStorage` 不可用（隐私模式等）时，自动降级为**内存存储**（页面内可用，刷新丢失），并在控制台提示——沿用原型防护逻辑，保证"页面永远可用"；
- **演进接缝（关键）**：接口设计为 **Promise 风格**——LocalAdapter 现在同步实现直接 resolve；未来多用户阶段新增 `RemoteAdapter` 用 HTTP 实现**同一契约**，stores/组件不感知存储实现，业务层零改动（详见 §17）；
- **ID 策略（演进接缝）**：任务 ID 使用 **UUID**（`crypto.randomUUID()`，兼容降级为手写 UUID v4）。单机时与时间戳方案无异，但多用户各自离线新增再合并时**全局唯一**，避免撞车；
- 预留：adapter 只暴露统一接口，未来切 IndexedDB 或加"导出/导入 JSON"不改业务层。

### 4.4 数据迁移策略（migrations）
- 启动时读取 `planApp.meta.v3.schemaVersion`；
- 若为旧版本（如原型 `planApp.tasks.v2`），执行迁移链：`v2 → v3`（结构兼容：直接改名搬移 + 补充默认字段 `updatedAt`、`version:1`、ID 转 UUID（缺失时生成））；
- `migrations.js` 维护 `[ {from:2,to:3,fn}, ... ]` 顺序执行，迁移前备份原 key（`planApp.tasks.v2.bak`）；
- 原型中的"示例种子"仅在**全新安装**（无任何 key）时写入，迁移用户数据时**不覆盖**；
- 未来加用户维度时通过同一条迁移链平滑升级（`v3 → v4`），不破坏本地数据。

### 4.5 派生查询（utils）
- `tasksForDate(iso)`：一次性按 date；固定单次按 date；固定重复/长期按星期匹配 `repeat`；结果按优先级+时间排序；
- `sortedForDayList` / `sortedForTasksView`：优先级（紧急>重要>普通）→ 开始时间 → 创建时间；
- `completedCount(task)` / `anyCompleted(task)`：清单页状态筛选用。

---

## 5. 状态管理（Pinia）

### 5.1 stores/tasks
```js
state: { tasks: [] }
getters: byId(id), tasksForDate(iso), allSorted
actions: init(加载+迁移+种子), addTask, updateTask, removeTask,
         toggleDone(id, iso), moveTaskTime(id, startMin, laneIdx), resizeTask(id, newDur)
```
> 每次 action 修改后**同步调用存储层写回**（本地单用户，无需防抖批量写）。

### 5.2 stores/notes
```js
state: { notes: {} }
actions: init, save(iso, text), remove(iso)
getters: notesOfMonth(year, month), weekGroups(year, month)
```

### 5.3 stores/ui
```js
state: { route, year, month, selectedDate(day), sideIso, sideOpen,
         filterStatus, filterType, notesYear, notesMonth,
         taskModal: { open, editId, defaultDate }, confirm: { open, title, msg, cb },
         user: null }        // 登录态占位【演进预留】：null=本地单机模式；
                             // 未来为 { id, name, … }，路由守卫/账号菜单基于它增量接入（见 §17）
```

---

## 6. 路由设计（hash）

| 路由 | 视图 | 说明 |
| --- | --- | --- |
| `#/calendar` | CalendarView | 默认首页；月历 + 侧边栏 |
| `#/day/2026-08-15` | DayView | 当天整页（列表 + 泳道时间线 + 总结） |
| `#/tasks` | TasksView | 任务清单 |
| `#/notes` | NotesView | 每日总结汇总（按周） |

- 未匹配 → 重定向 `#/calendar`；`#/day/<非法日期>` → 归一化到合法日期；
- 顶栏"今日详情" → `#/day/<今天>`；进入 day 视图时高亮该 Tab；
- hash 模式对"双击构建产物"与"本地服务"均可用。

---

## 7. 页面与组件设计

### 7.1 组件树（核心）
```
App
├─ AppHeader            # 品牌 / Tab 导航 / 今日进度 / 预览激励
├─ RouterView
│  ├─ CalendarView
│  │  ├─ DayCell[]      # 公历+农历+宜忌+优先级色标签
│  │  └─ SidePanel      # 当天任务预览（勾选/编辑/删除 + 查看详情）
│  ├─ DayView
│  │  ├─ DayHeader      # 日期/农历/宜忌/进度 + 添加按钮(最右) + 时钟
│  │  ├─ DayTaskList    # 当天任务逐行（勾选/编辑/删除）
│  │  ├─ LaneTimeline   # ★ 泳道式时间线（见 §8）
│  │  │  ├─ AllDaySection
│  │  │  └─ TimelineBar[]  # 任务矩形框（含 resize 把手）
│  │  └─ DailyNote      # 每日总结编辑区
│  ├─ TasksView
│  │  └─ TaskRow[]      # 筛选/排序/展开详情/编辑/删除
│  └─ NotesView
│     └─ WeekGroup[]    # 按周分组（点击"未写"跳当天页补写）
├─ DanmakuStage         # 底部弹幕舞台（fixed，不覆盖内容）
└─ 全局弹窗
   ├─ TaskFormModal     # 新增/编辑任务（复用，预填）
   └─ ConfirmDialog     # 删除确认
```

### 7.2 关键组件职责与接口（节选）
| 组件 | props | emits | 职责 |
| --- | --- | --- | --- |
| `DayCell` | `iso, year, month, today` | `select(iso)` | 渲染农历/宜忌/任务标签，今日高亮 |
| `SidePanel` | `iso` | `close, goDetail` | 预览列表；操作后**由 store 触发即时刷新** |
| `LaneTimeline` | `iso` | — | 泳道布局 + 拖拽/调整时长（详见 §8） |
| `TimelineBar` | `task, iso, pixel` | `drag…` | 矩形框渲染（时间/标题/按钮/把手） |
| `TaskFormModal` | `open, editId, defaultDate` | `submit, cancel` | 三类任务表单 + 优先级 + 备注；编辑预填 |
| `ConfirmDialog` | `open, title, message` | `confirm, cancel` | 删除等危险操作确认 |
| `DanmakuStage` | — | — | 监听 `danmaku:push` 事件，底部升起淡出 |

---

## 8. 泳道式时间线组件详细设计（核心）

### 8.1 布局参数（常量，集中管理）
| 参数 | 值 | 说明 |
| --- | --- | --- |
| `HOUR_W` | 90px | 1 小时横向宽度（24h = 2160px，横向滚动） |
| `LANE_H` | 64px | 泳道行高（矩形框高 = 58px） |
| `TOP` | 36px | 时间轴顶部刻度区高度 |
| `SNAP_MIN` | 15 分钟 | 拖拽/调整时长吸附步长 |
| `MIN_DUR` | 15 分钟 | 矩形框最小时长 |
| `MIN_W` | 64px | 矩形框最小宽度 |

### 8.2 时间-像素映射
```
x(px)  = startMinutes / 1440 * (24*HOUR_W)
width  = taskDur(task) / 60 * HOUR_W        （>= MIN_W）
y      = TOP + laneIndex * LANE_H + 3
```

### 8.3 泳道分配算法（`utils/timeline.js`，纯函数可单测）
```
输入：当天定时任务（按开始时间排序）
输出：每个任务 → { lane, st, en }
算法：贪心
  lanes = []
  for t in tasks:
    pref = t.laneIdx ?? 0
    lane = pref
    while lanes[lane] 中存在与 [st,en) 重叠的任务: lane++
    lanes[lane].push({st,en})
```
- 无开始时间的任务**不进时间轴**，进入上方 `AllDaySection`（独立区，横向滚动时始终可见）；
- 拖拽换泳道 = 修改 `laneIdx` 后重跑算法（以 `laneIdx` 为偏好、冲突自动顺延）。

### 8.4 全天区（AllDaySection）
- 位于时间轴容器**上方**（普通文档流，不随横向滚动）；
- 每任务一行：`[全天] 标题 [✓][🗑]`，优先级左边条、完成置灰划线；
- 不提供时间拖拽（无时间可调）。

### 8.5 拖拽交互状态机（`useTimelineDrag`）
```
状态：idle → moving | resizing → idle
pointerdown 目标判定（按命中优先级）：
  1) .mini 按钮        → 不拖拽（交给点击）
  2) .tl-resize 把手    → resizing（调整时长）
  3) .tl-task 主体      → moving（移动/换泳道）
pointermove：
  moving   → left/top 实时跟随（换算 startMinutes 与目标泳道预览）
  resizing → width 实时跟随（换算新时长，吸附 15min，≥MIN_DUR，≤24:00）
pointerup：
  moving   → 写 startTime（endTime 平移保持时长）、laneIdx，持久化
  resizing → 写 durationMin（fixed 任务）+ endTime，持久化
```
- 监听器挂 `document`，`pointerup` 后移除；组件卸载时清理；
- 重复渲染**先移除旧监听再绑定**（原型中已修复的监听器累积问题，组件化后由 `onUnmounted` 兜底）。

### 8.6 当前时间指示
- `setInterval` 每秒：更新顶部时钟；更新红色"现在"竖线 `left`（按当前分钟）；
- 打开当天页时 `scrollLeft` 自动定位到当前时刻；
- 离开 day 视图时清理定时器；`prefers-reduced-motion` 时静默。

---

## 9. 交互设计规范

| 交互 | 规范 |
| --- | --- |
| 完成任务 | 勾选 = 立即完成/取消（马上变灰划线，无弹窗），触发夸赞弹幕 |
| 修改任务 | 独立编辑弹窗（当天页列表/侧边栏/清单页均有入口），全字段预填，保存保留 id/完成记录 |
| 删除任务 | ConfirmDialog 确认（"删除后不可恢复，并从所有日期移除"） |
| 新增按钮 | 当天页**日期栏最右**；清单页工具栏也有"新建任务" |
| Esc | 依次关闭：任务弹窗 → 删除确认 → 侧边栏 → 当天页返回日历 |
| 遮罩点击 | 关闭对应弹窗/侧边栏 |
| 弹幕触发 | 仅 3 时机：打开侧边栏/进入当天页（查看激励）、完成任务（夸赞）、保存任务（鼓励）；底部舞台、升起淡出约 3.2s、不覆盖内容 |
| 键盘可达 | 全站 Tab 可导航、焦点环可见；弹窗打开聚焦首字段 |

---

## 10. 视觉设计系统（老黄历 tokens）

### 10.1 颜色
| Token | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#F2E8D0` | 页面纸感底色（叠加细纹） |
| `--surface` | `#FBF4E3` | 卡片/面板 |
| `--surface-2` | `#F1E7CE` | 次级面（输入/禁用） |
| `--border` / `--border-strong` | `#CBB98A` / `#B9A26B` | 边框 |
| `--text` / `--text-soft` / `--text-faint` | `#3B362C` / `#6E6350` / `#94876B` | 文字三级 |
| `--primary` | `#A03028` | 朱砂红（主操作/强调） |
| `--on-primary` | `#FFF8EC` | 主色上的文字 |
| `--accent` / `--accent-soft` | `#2E7D5B` / `#E2EFE7` | 完成/成功 |
| `--danger` / `--danger-soft` | `#A03028` / `#F6E3E0` | 危险/删除 |
| `--urgent` / `--important` / `--normal` | `#B33A2E` / `#C98A1B` / `#5E748C` | 优先级红/橙/蓝灰 |
| `--yi` / `--ji` | `#A03028` / `#6B6454` | 宜（红）/ 忌（墨灰） |
| `--stage-bg` | `#EFE3C4` | 弹幕舞台 |

### 10.2 字体 / 圆角 / 间距 / 动效
- 标题字体：楷体栈 `'Kaiti SC','STKaiti','KaiTi','楷体','Noto Serif SC',serif`；正文：宋体栈；
- 圆角：`--radius:8px`（卡片）、`--radius-sm:6px`（控件）；
- 间距：4 的倍数网格（4/8/12/16/24）；
- 阴影：`0 3px 10px rgba(90,70,30,.18)`（纸面投影，克制）；
- 动效：交互 150–300ms ease；弹幕 3.2s 升起淡出；全局尊重 `prefers-reduced-motion`；
- 背景：纸感细纹（`repeating-linear-gradient`）+ 朱砂红标题/顶栏分隔线。

---

## 11. 农历与黄历模块（utils/lunar.js）

- **农历换算**：经典 `lunarInfo` 表（1900–2049，150 项）+ `solar2lunar`（已用 2026-08-18=七月初六、2000-01-01=戊午 等基准验证）；
- **干支**：年 = 农历年 `(y-4)%10/(y-4)%12`；日 = 儒略日公式 `(JDN+49)%60`；
- **宜忌**：原型沿用 10 组循环示例表（`YIJI`，按干支日取模）；设计上**抽成数据模块**，正式版可替换为真实黄历数据源（接口 `yiJiOf(iso) → {yi, ji}`）；
- 页面展示：日历格农历小字+宜忌、当天页农历+干支+宜忌。

---

## 12. 性能与可访问性设计

| 维度 | 方案 |
| --- | --- |
| 性能 | 本地小数据量，全量重绘即可；时间线渲染仅当天任务（≤几十条）；`setInterval` 仅在 day 视图激活时运行 |
| 响应式 | 375 / 768 / 1024 / 1440px；移动端：任务标签变圆点、侧边栏全宽、时间轴触屏拖拽（pointer 事件） |
| 键盘 | 全交互可键盘完成；Esc 层级关闭；焦点环 `:focus-visible` |
| 对比度 | 正文 ≥ 4.5:1（`--text` 对 `--bg` 达标）；图标按钮带 `aria-label` |
| 减弱动效 | `prefers-reduced-motion`：弹幕瞬间完成、过渡归零 |
| 语义 | 日历 `role=grid/gridcell`、弹窗 `role=dialog/alertdialog`、弹幕层 `aria-hidden` |

---

## 13. 测试方案

| 层 | 工具 | 覆盖点 |
| --- | --- | --- |
| 单元 | Vitest | `lunar.js`（基准日校验）、`date.js`、`timeline.js`（泳道分配/重叠）、`migrations.js`（v2→v3）、`priority.js` |
| 组件 | Vue Test Utils | TaskFormModal（新增/编辑/校验）、LaneTimeline（渲染/拖拽/调整时长）、SidePanel（勾选即时变灰）、DanmakuStage（触发时机） |
| 端到端（可选） | Playwright | 主链路：新增→泳道可见→勾选变灰→编辑→删除确认→按周汇总→刷新持久化 |

**验收映射**（需求文档 §7 验收点 → 测试用例一一对应，作为 M3/M4 完成标准）。

---

## 14. 实施计划

| 阶段 | 内容 | 交付物 | 验收标准 |
| --- | --- | --- | --- |
| M2 骨架 | Vite 工程、路由、tokens/base.css、存储层（adapter+迁移，**接口 Promise 化、ID 用 UUID、模型含 version**）、stores、农历/日期工具 | 可运行空壳 + 数据层 | 启动脚本起服务；迁移 v2 数据；单测通过 |
| M3 四视图 | 日历+侧边栏、当天页（列表+全天区+泳道时间线+拖拽/调整时长+时钟+总结）、清单、按周汇总；任务表单（新增/编辑/校验）、删除确认 | 功能与原型 1:1 | 需求文档验收点全过（组件+单元测试） |
| M4 体验 | 弹幕系统、动效打磨、今日进度、预览激励 | 完整正式版 | 弹幕三时机正确；动效规范 |
| M5 收尾 | 无障碍走查、响应式走查、数据导出/导入 JSON、构建产物与双击兼容 | 发布版 | 375px 无横向滚动；键盘可达；导出/导入可用 |

依赖：M3 依赖 M2；M4 依赖 M3；M5 依赖 M4。每阶段结束提交可运行版本并回归验收映射表。

---

## 15. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| localStorage 受限/被清除 | 数据丢失 | adapter 降级内存 + 明确提示；M5 提供导出/导入 JSON 备份 |
| 拖拽交互在触屏不跟手 | 体验下降 | pointer 事件 + `touch-action:none`；M4 真机走查 |
| 老黄历字体在不同系统缺字 | 标题回退字体差异 | 字体栈多层回退（楷体→宋体→serif） |
| 泳道任务多时高度过大 | 页面过长 | 泳道贪心算法保证最少行数；远期可加"折叠更多" |
| 原型数据被覆盖 | 用户已有任务丢失 | 迁移只读不改写旧 key，先备份后写入新 key |
| **未预留存储异步接口** | 未来切后端需改动所有调用点 | **M2 即按 Promise 契约实现**（§4.3） |
| **本地 ID 撞车** | 多设备合并时数据冲突 | **M2 即用 UUID**（§4.3） |
| **未来并发编辑冲突** | 多人改同一任务互相覆盖 | 模型带 `version`，后端阶段用乐观锁（§17） |

---

## 16. 附录：需求-设计映射

| 需求（v1.3） | 设计方案落点 |
| --- | --- |
| 日历+侧边栏+查看详情 | §6/§7（CalendarView、SidePanel） |
| 当天页：列表+泳道时间线+总结 | §7/§8（DayView、LaneTimeline、DailyNote） |
| 全天任务始终可见 | §8.4（AllDaySection 独立区） |
| 矩形框拖拽移动/换泳道 | §8.5（moving） |
| 矩形框调整时长（右边缘） | §8.5（resizing）+ TimelineBar 把手 |
| 勾选即完成变灰、修改独立 | §9（toggleDone / TaskFormModal） |
| 优先级三级 | §4.1 + tokens（--urgent/--important/--normal） |
| 删除确认 | §9（ConfirmDialog） |
| 顶栏今日详情 | §6 路由 + AppHeader |
| 弹幕三时机底部舞台 | §9 + DanmakuStage |
| 持久化/重启恢复 | §4.3/§4.4（adapter + migrations） |
| 老黄历视觉 | §10 tokens + 农历模块 §11 |
| 多用户/并发/账号演进预留 | §2.4/§4.3/§5.3/§17 |

---

## 17. 架构演进与多用户预留

> 目标：当前为**本地单用户**，但为未来**多用户、并发、账号机制**预留架构接缝——**当前不实现**，仅保证演进路径平滑、不与现有架构冲突。

### 17.1 演进原则
1. **接缝先行**：凡是未来要换实现的边界（存储、ID、并发、登录），现在就把接口/字段留好；
2. **业务层不感知存储实现**：视图与 store 只依赖"统一契约"，本地/远端切换不触碰业务代码；
3. **本地数据干净**：单机模型不带用户维度，多用户归属由后端按会话隔离；
4. **能力后补、不提前引入复杂度**：后端、认证、同步协议均不在 M2–M5 实现。

### 17.2 演进路径（三阶段）
```
阶段1  本地单机（现在，M2–M5）
        Vue 3 + LocalAdapter（Promise 契约）+ UUID + version + user=null
        ↓ 需要多设备/多人时（后议，不改现有代码）
阶段2  个人多设备同步
        引入 RemoteAdapter（HTTP，同一契约）+ 个人后端（Node.js + SQLite）
        账号从"设备密钥/简单密码"起步
        ↓ 需要团队协作时
阶段3  小团队多用户
        完整账号体系（注册/登录/JWT）+ 按用户隔离 + 乐观锁冲突合并
```

### 17.3 已预留的接缝（对照）
| 接缝 | 现状（M2 落实） | 未来接入 | 冲突风险 |
| --- | --- | --- | --- |
| 存储接口（§4.3） | Promise 契约 + LocalAdapter | RemoteAdapter 同契约 | 无（业务层只依赖接口） |
| 任务 ID（§4.3） | UUID | 直接使用，全局唯一 | 无 |
| 并发控制（§4.1） | `version` 字段（=1） | 乐观锁比对 + 冲突提示合并 | 无 |
| 登录态（§5.3） | `user = null` | 登录后注入；路由守卫、账号菜单增量接入 | 无 |
| 数据归属（§4.1） | 本地模型无 owner | 后端按会话隔离 | 无 |
| schema 版本（§4.4） | 迁移链 v3 | v3→v4 平滑升级 | 无 |

### 17.4 并发与冲突设计原则（未来）
- **乐观并发控制**：每次写入携带 `version`，后端比对；版本不符返回冲突；
- **冲突处理**：前端提示用户三选一——保留本地 / 采用远端 / 手动合并；不引入实时协同（复杂度高，个人/小团队场景不需要）；
- **同步粒度**：任务级（单条任务冲突处理），不做字段级 diff。

### 17.5 后端选型建议（未来，两个方向兼容）
| 场景 | 建议后端 | 说明 |
| --- | --- | --- |
| 个人多设备同步 | Node.js + Express/Fastify + **SQLite** | 轻量、单文件库，可跑云主机/家用服务器；数据表结构与前端任务 JSON 对应 |
| 小团队多用户 | 同上 + PostgreSQL（可选）+ 账号体系（用户名/密码 + JWT） | 并发与权限更强 |

> 共同点：后端只需实现与前端 `storage/adapter.js` **同一份接口契约**（§4.3），前端零改动切换；API 形态（REST/JSON-RPC）届时按团队偏好定，当前不写接口草案（已确认"后端后议"）。

### 17.6 未来接入核对清单（届时执行）
- [ ] 新增 `storage/remote/` 实现 RemoteAdapter，逐方法对照 §4.3 契约；
- [ ] 登录页 / 路由守卫 / 账号菜单增量接入（基于 `ui.user`）；
- [ ] 迁移链新增 `v3 → v4`（如引入 owner 索引字段）；
- [ ] 后端实现乐观锁（version 比对）与冲突响应；
- [ ] 双实现（Local/Remote）跑通同一套测试（§13）。
