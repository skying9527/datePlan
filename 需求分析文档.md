# 任务规划与记录网页 —— 需求分析文档

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 任务规划与记录网页（原型名：拾光任务） |
| 文档版本 | v1.3 |
| 文档状态 | 已定稿：老黄历皮肤 + 泳道式时间线；本轮修复/调整待验收 |
| 交付物 | ① 本需求分析文档 ② 最终原型 `原型A-老黄历-泳道式/index.html` |

## 版本历史

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 首版 | 需求澄清 + 单版原型 |
| v1.1 | 上上轮 | 7 条修改：弹幕/多皮肤/清单/总结/优先级/删除确认/每日整页 |
| v1.2 | 上轮 | 选定老黄历；侧边栏、当天页横向时间线（A/B/C 三变体）、编辑、新增按钮位置 |
| v1.3 | 本轮 | **定稿泳道式时间线**；修复 4 项问题；顶栏新增"今日详情"入口 |

---

## 1. 项目概述

本地部署、纯前端的个人任务规划与记录网页。最终方案：**老黄历皮肤 + 泳道式横向时间线**。

- 日历（月视图）→ 点击某天弹**侧边栏**（预览/勾选/编辑/删除）→ "查看当天详情"进入**当天整页**；
- 当天页：顶部日期栏（含**添加当天任务**按钮，最右）→ **今天任务列表** → **泳道式横向时间线**（所有任务以小矩形框展示，时间重叠的上下分层不遮挡，可拖拽调整）→ 每日总结；
- 三类任务（一次性/固定时长/长期），均支持**新增、修改、删除（确认）、完成**；
- **完成与修改分开**：勾选 = 立即完成/取消（马上变灰），修改 = 编辑弹窗（独立入口）；
- localStorage 持久化；底部弹幕激励（仅查看某天/完成任务/保存任务触发）。

### 本轮（v1.3）调整与修复
| # | 内容 | 方案 |
| --- | --- | --- |
| 1 | 时间线定稿 | 选择**泳道式**（变体 A），移除 B/C 变体 |
| 2a | 新增任务泳道不展示 | 无开始时间的任务进入**"全天"泳道行**，所有当天任务均在泳道可见 |
| 2b | 侧边栏无修改按钮、勾选不立即变灰 | 侧边栏任务行增加**编辑按钮**；勾选/取消/删除后**侧边栏立即刷新**（马上变灰） |
| 2c | 修改与完成分开 | **勾选 = 立即完成/取消完成**（无弹窗、立即变灰）；修改 = 独立编辑弹窗；移除"完成情况"弹窗（心得可写备注/每日总结） |
| 2d | 新增一次性任务优先级不可选 | 加固优先级选择（打开弹窗时清理禁用/选中状态，选中态高亮 + aria-pressed） |
| 3 | 顶栏入口 | 在"每日总结"后增加**"今日详情"**链接，点击直达当天详情页 |

---

## 2. 需求澄清记录

沿用 v1.0–v1.2 已确认结论：三类任务定义、localStorage 持久化、优先级三级（紧急红/重要橙/普通蓝灰）、每日总结按周分组、删除需确认、弹幕三时机底部舞台、编辑全部字段可编辑、当天页布局（列表在上时间线在下）、新增按钮在日期栏最右。
v1.3 追加：**时间线定稿泳道式**；**完成=立即勾选变灰、修改=独立编辑**。

---

## 3. 功能需求

### 3.1 日历视图
- 7 列月网格（周一起始），翻月/回到今天，今天高亮、非当月灰显；
- 日期格：公历 + 农历 + 宜/忌 + 优先级色任务标签（紧急红/重要橙/普通蓝灰），已完成置灰划线，超出折叠 +N；
- **点击某天 → 侧边栏**：日期+农历+宜忌、当天任务列表（**勾选立即完成变灰 / 编辑 / 删除**）、"查看当天详情"跳转整页；遮罩/ESC 关闭；操作后即时刷新；打开时触发查看激励弹幕。

### 3.2 当天详情页（整页）
1. 顶部栏：返回日历、前一天/今天/后一天、实时时钟；
2. 日期栏：公历+农历+干支+宜忌、今日进度、**最右：添加当天任务按钮**；
3. 今天任务列表：逐行展示（优先级色条、**勾选=立即完成变灰**、标题、时间/类型、**编辑**、删除）；
4. **泳道式时间线**（3.4）；
5. 每日总结编辑区。

### 3.3 任务管理
- 三类任务 + 优先级 + 备注 + 按天完成记录（完成时自动记时间戳，立即变灰）；
- **修改**：当天页列表、侧边栏、任务清单页均有编辑入口；编辑弹窗预填全部现值（类型/日期/起止时间/时长/重复频率/优先级/备注），保存保留 id 与已完成记录；
- 删除：确认弹窗（不可恢复警示）。

### 3.4 泳道式时间线（定稿）
- 横向 0–24 时时间轴（1 小时 = 84px，可横向滚动），每 2 小时刻度；
- **所有当天任务均为小矩形框**：有开始时间的按时间定位；**无开始时间的进入顶部"全天"泳道行**（标"全天 · 标题"），保证新增任务一定可见；
- 时间重叠的任务**自动分到上下泳道行**（矩形框上下排列、不遮挡）；优先级色左边条（紧急红/重要橙/普通蓝灰）；完成后置灰划线；
- **拖拽**：左右拖 = 改开始时间（吸附 15 分钟，时长不变）；上下拖 = 换泳道；全天任务不可左右拖（仅可上下换行）；
- **当前时间红色竖线**实时移动，打开时自动滚动到当前时刻；
- 矩形框内可直接**勾选完成（立即变灰）**与**删除（确认）**。

### 3.5 顶栏导航
Logo · Tab[日历 | 任务清单 | 每日总结 | **今日详情**] · 今日进度 · 预览激励。"今日详情"点击直达今天详情页（当天页时高亮）。

### 3.6 激励弹幕 / 持久化（沿用）
- 触发：进入当天页/打开侧边栏（查看激励）、**完成任务**（夸赞）、保存任务（鼓励）；
- localStorage：`planApp.tasks.v2`、`planApp.notes.v2`。

---

## 4. 非功能需求（沿用）

老黄历视觉（纸感/朱砂红/楷体/农历宜忌）；响应式 375/768/1024/1440px；Esc 关闭；焦点可见；对比度 ≥4.5:1；pointer 拖拽兼容触屏；动效 150–300ms 且尊重减弱动效。

---

## 5. 数据模型（沿用 v1.2）

```json
{
  "id": "唯一ID", "title": "写周报",
  "type": "once | fixed | long",
  "date": "2026-08-15", "startTime": "09:00", "endTime": "10:00",
  "durationMin": 30, "mode": "once | repeat",
  "repeat": { "kind": "daily" } | { "kind": "weekly", "days": [1,3,5] },
  "priority": "urgent | important | normal",
  "note": "备注",
  "completed": { "2026-08-15": { "at": "ISO", "note": "" } },
  "order": 0, "laneIdx": 0,
  "createdAt": "ISO"
}
```
> v1.3：`completed[日期].note` 保留字段（兼容），完成时默认空；心得写备注/每日总结。

---

## 6. 页面结构（v1.3）

```
顶栏：Logo · Tab[日历|任务清单|每日总结|今日详情] · 今日进度 · 预览激励
视图1 日历：月网格 → 点击某天 → 侧边栏（勾选/编辑/删除 + 查看当天详情）
视图2 当天页：顶部栏 → 日期栏[添加当天任务 最右] → 今天任务列表
                → 泳道式时间线（全天泳道 + 定时泳道 + 拖拽 + 当前时间红线）→ 每日总结
视图3 任务清单：筛选 + 紧急优先 + 展开详情 + 编辑 + 删除(确认)
视图4 总结汇总：月份切换 + 按周分组
底部弹幕舞台
弹窗：新增/编辑任务 · 删除确认
```

---

## 7. 验收要点（v1.3 更新）

- [ ] 新增任务（含不填时间）→ 当天泳道**立即可见**（无时间显示在"全天"泳道行）。
- [ ] 侧边栏：有编辑按钮；勾选/取消/删除后**立即变灰/刷新**。
- [ ] 勾选 = 立即完成变灰（无弹窗）；修改 = 编辑弹窗，两者独立。
- [ ] 新增一次性任务时优先级（紧急/重要/普通）可选且保存正确。
- [ ] 顶栏"今日详情"在每日总结后，点击进入今天详情页。
- [ ] 重叠任务上下分层不遮挡；左右拖改时间（吸附15分钟）、上下拖换泳道。
- [ ] 其余回归：筛选/编辑预填/删除确认/按周汇总/总结保存/弹幕三时机/持久化。

---

## 8. 里程碑建议

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M1 | 需求分析 + 原型迭代 | v1.3：**方案已定稿，待验收** |
| M2 | 正式版骨架 + 存储层 + 设计系统（老黄历 + 泳道时间线） | 待启动 |
| M3 | 四视图 + 拖拽泳道组件 | 待启动 |
| M4 | 弹幕激励 + 动效打磨 | 待启动 |
| M5 | 收尾：无障碍、响应式、数据导出 | 待启动 |
